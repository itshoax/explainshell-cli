## After cloning the repo just run the script install.sh
`bash install.sh` or `./install.sh`

## The script will install everything and now you can run the explaination with the following
`explain <command>`

e.g. `explain ls`
